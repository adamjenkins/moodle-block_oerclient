# Release notes — 0.1.0

Initial alpha release: a combined Dashboard block showing this site's own
outgoing shares to the OER Exchange ("What I've shared") and the newest
resources published on the connected Exchange ("Recent OER available").
Declares a hard `version.php` dependency on `local_oerclient`.
